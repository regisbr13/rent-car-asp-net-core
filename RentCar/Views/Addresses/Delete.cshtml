@model RentCar.Models.Address

@{
    Layout = null;
    ViewData["Title"] = "Deletar endereço";
}

<div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLongTitle">Excluir endereço</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="modal-body">
                <p>
                    Deseja mesmo excluir o endereço?
                </p>
                <p>
                    Rua <strong>@Model.Street</strong>, n° <strong>@Model.Number</strong> - <strong>@Model.District</strong>, <strong>@Model.City</strong> - <strong>@Model.State</strong>
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary btn-sm" data-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary btn-sm">Salvar</button>
            </div>
        </form>
    </div>
</div>

@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
